CODE _find  ( cur w -- a f )
    HL POP,       ( w )
    DE POP,       ( cur )
    chkPS,
    lblfind @ CALL,
    IFNZ,
        ( not found )
        HL PUSH,
        PUSH0,
        JPNEXT,
    THEN,
    ( found )
    DE PUSH,
    PUSH1,
;CODE
