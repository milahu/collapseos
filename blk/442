: EMIT
    ( 0x53==EMITPTR 0x55==override )
    85 RAM+ @ DUP NOT IF DROP 83 RAM+ @ THEN EXECUTE ;

: (print)
    BEGIN
    C@+      ( a+1 c )
    ( exit if null )
    DUP NOT IF 2DROP EXIT THEN
    EMIT     ( a )
    AGAIN
;

